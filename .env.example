# Copier en .env et renseigner des valeurs réelles avant `docker compose up`.
# .env est dans .gitignore : ne jamais committer de secret réel.
JWT_SECRET=change-me-please-use-a-long-random-value-min-32-bytes